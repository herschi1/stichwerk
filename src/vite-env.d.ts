/// <reference types="vite/client" />

/** From package.json, injected at build time (see vite.config.ts). */
declare const __APP_VERSION__: string;
/** ISO timestamp of the build. */
declare const __BUILD_TIME__: string;
